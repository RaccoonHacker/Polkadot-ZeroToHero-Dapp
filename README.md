# Polkadot Dapp(Zero to Hero)

使用 viem 和 Next.js 构建了一个功能齐全的 dApp，该 dApp 可与 Polkadot Hub 上的智能合约进行交互。

1 使用 Hardhat 创建智能合约并将其部署到 Polkadot Hub 测试网。

2 连接到用户的钱包并处理网络切换。

3 从智能合约中读取数据并保持更新。

4 通过交易将数据写入区块链。

5 更新一个数字存储到区块链
